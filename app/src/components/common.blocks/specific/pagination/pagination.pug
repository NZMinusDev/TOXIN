//- id: id of button
//- class: class for component bem mix: such as positioning functionality or size
//- pages - amount of pages for click
//- page - start page
//- amount-to-display - amount to display such as 12/24/48/96
//- total-amount - amount of pages
//- text - placed text after amounts
mixin pagination
  nav.pagination(
    id=attributes.id,
    class=attributes.class,
    data-pages=attributes.pages,
    data-page=attributes.page,
    data-amount-to-display=attributes['amount-to-display'],
    data-total-amount=attributes['total-amount'],
    data-text=attributes.text
  )
    //- here would be items generated by pagination.ts
    //- FIXME: inside pug template we should use buffered code(!=) of creation function, that returns string. But my compiler alarms when see "onclick"(says: "createPagination isn't defined) or "document"(says: "Cannot read property 'createElement' of undefined") in buffered mod but works good if unbuffered mod .-.
